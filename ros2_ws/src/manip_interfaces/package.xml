<?xml version="1.0"?>
<package format="3">
  <name>manip_interfaces</name>
  <version>0.1.0</version>
  <description>
    Service/message interfaces for the perception sidecars (trellis2, and
    eventually pose/pointso once their topic-JSON interfaces are promoted).
    ament_cmake on purpose: rosidl generation does not work under
    ament_python, so all .srv/.msg for the whole stack live here and the
    Python nodes stay in manip_bridge.
  </description>
  <maintainer email="albus@localhost">albus</maintainer>
  <license>MIT</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <depend>sensor_msgs</depend>
  <depend>shape_msgs</depend>
  <depend>geometry_msgs</depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>